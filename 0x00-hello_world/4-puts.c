#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints a message to the screen using the puts function
 *              Takes no input parameters and returns 0 when it is finished.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
